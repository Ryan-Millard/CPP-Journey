#include "../std_lib_facilities.h"

int main()
{
	cout<<"Hello programming!\nHere we go!\n";
	// keep_window_open();		needed on some Windows machines to prevent them from closing the window before you have a chance to read the output.
	return 0;
}
